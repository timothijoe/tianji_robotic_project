# 背景意图

本项目是一个最小但可扩展的机器人可视化与控制链路。核心目标是：用 MuJoCo 作为唯一运行时控制后端，用 ROS2/RViz 作为可视化与消息接入层，并用统一的内部接口承接未来不同自由度机器人模型和可替换规划算法库。

完整链路说明见 `docs/ARCHITECTURE.md`。本文只保留最精炼的背景意图和领域词汇。

## 领域词汇

- 机器人资产：URDF、MJCF/XML、STL mesh 与关节元数据。URDF 用于 ROS/RViz 展示，MJCF/XML 用于 MuJoCo 运行时控制。
- 关节状态：一组关节名和对应位置、速度、力矩、时间戳。
- 关节轨迹：按时间排序的关节目标点序列，是规划器、控制器和 ROS 消息之间的统一交换形式。
- 示教器：面向调试和演示的 GUI 关节目标入口，消费当前关节状态并输出统一关节轨迹。
- 规划器：从起点状态和目标状态生成关节轨迹的模块。当前默认线性插值，未来可替换为 OMPL 或其他算法库。
- 控制器：消费控制命令并更新 MuJoCo 状态的模块。当前只保留 MuJoCo position controller。
- ROS 适配：把标准 ROS 消息转换为内部接口类型，核心规划/控制不直接依赖 rclpy。
- 功能包：`cook_core` 放通用接口，`cook_description` 放机器人资产，`cook_mujoco` 放 MuJoCo 控制，`cook_bringup` 放 ROS2 接入与可视化。

## 设计原则

- 运行时只加载预生成 MJCF/XML，不在线转换 URDF。
- 核心接口使用 Python 数据类型，ROS 只作为 Adapter。
- 新机器人通过资产和关节元数据接入，不要求修改控制链路。
- 新算法库通过规划器 Interface 接入，不要求修改 MuJoCo 控制器。
- 测试通过公共 Interface 验证行为，不绑定内部实现细节。
